import { Colour } from '../../types/Colour';

export const DEFAULT_BACKGROUND: Colour = {
  name: 'Light purple',
  hex: '#ceb4fd',
};

export const OUTPUT_SIZE = 900;
export const FONT_SIZE = 48;
export const SPACING = 64;
