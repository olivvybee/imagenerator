import { TextSetting } from '../../types/SettingTypes';

export type IHaveNoXSettings = {
  x: TextSetting;
  y: TextSetting;
};

export type IHaveNoXCache = {
  font: FontFace;
  background: HTMLImageElement;
};
